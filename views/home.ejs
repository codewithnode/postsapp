<%- include("./partials/header", {pageTitle: "Home"}) %>
    <h4>Welcome to Posts App</h4>
    <!-- <% if (loggedout) { %>
    <a href="/login">Login</a>
    <% } else { %>
    <a href="/logout">Logout</a>
    <p>Create a new post <a href="/createpost">Create</a></p>
    <% } %> <% if (loginstatus) { %> <% } %> -->
    <% posts.forEach( post => { %> 
    <br /><br />
    <div class="card">
      <div class="container">
        <h4><%= post.post %></h4>
        <p>by <%= post.author %></p>
        <a href="/editpost/<%= post.id %>">Edit</a> ||
        <a href="/deletepost/<%= post.id %>">Delete</a>
      </div>
    </div>
    <% }) %>
  </body>
</html>
